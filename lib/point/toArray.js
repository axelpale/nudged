module.exports = (p) => {
  // @nudged.point.toArray(p)
  //
  // Convert a point `{ x, y }` into a two-element array `[x, y]`.
  //
  // Parameters
  //   p
  //     a point
  //
  // Return
  //   an array `[x, y]`
  //
  return [p.x, p.y]
}
