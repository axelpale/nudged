module.exports = (p, q) => {
  // Thest if the coordinates of two points are strictly equal.
  //
  // Parameters:
  //   p
  //     a point
  //   q
  //     a point
  //
  // Return
  //   a boolean
  //
  return (p.x === q.x && p.y === q.y)
}
